class ChaosRenderer{constructor(canvas){this.canvas=canvas;this.ctx=canvas.getContext("2d");this.points=[];this.game=null;this.resize();new ResizeObserver(()=>this.resize()).observe(canvas)}resize(){const r=this.canvas.getBoundingClientRect(),d=Math.min(2,devicePixelRatio||1);this.canvas.width=Math.max(1,Math.round(r.width*d));this.canvas.height=Math.max(1,Math.round(r.height*d));this.drawAll()}setGame(game){this.game=game;this.points=[];this.drawAll()}add(point){this.points.push({...point});this.dot(point)}dot(p){const c=this.ctx;c.fillStyle="#0868f7";c.fillRect(Math.round(p.x*this.canvas.width),Math.round(p.y*this.canvas.height),Math.max(1,devicePixelRatio||1),Math.max(1,devicePixelRatio||1))}drawAll(){const c=this.ctx;c.fillStyle="#fbfdff";c.fillRect(0,0,this.canvas.width,this.canvas.height);if(!this.game)return;for(const p of this.points)this.dot(p);c.fillStyle="#ed5364";for(const v of this.game.polygon.vertices){c.beginPath();c.arc(v.x*this.canvas.width,v.y*this.canvas.height,4*Math.min(2,devicePixelRatio||1),0,Math.PI*2);c.fill()}}download(){const a=document.createElement("a");a.download="chaos-game.png";a.href=this.canvas.toDataURL("image/png");a.click()}}
